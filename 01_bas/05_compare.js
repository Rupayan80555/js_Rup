//   The equality check == and comparisons < > >= <=works differently
//  like comparisons converts null to number, treating it as 0. 

// === is strictly check it checks not only values as well as datatypes.